<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('narrations', function (Blueprint $table) {
            $table->id();
            $table->morphs('narratable'); // narratable_type + narratable_id
            $table->string('slot', 30)->default('intro'); // intro, content, summary, etc.
            $table->text('text');
            $table->string('audio_path')->nullable();
            $table->string('language', 5)->default('en');
            $table->string('voice')->nullable();
            $table->timestamps();

            $table->index(['narratable_type', 'narratable_id', 'slot']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('narrations');
    }
};